credentials.json
token.json
*credentials*.json
*secret*.json
token_*.json
drive_scout_output/
file_metadata_*.xlsx
__pycache__/
*.pyc
.env

# email_pipeline runtime artifacts — never commit these
email_pipeline/Email_Body_Processing_Config.json
email_pipeline/*.xlsx
email_pipeline/*.json

# Local Claude Code config — machine-specific
.claude/
.superpowers/

# Working output / data artifacts — keep locally, never commit
output/

# Database files — large, live in Litigation\Pipeline
*.db

# Audio pipeline artifacts
audio_pipeline/.pytest_cache/
audio_pipeline/speakers/
audio_pipeline/transcripts/
